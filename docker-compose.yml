version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: growthnote-app
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_HTTP_PORTS: 8080
      ConnectionStrings__Default: "Server=host.docker.internal,1433;Database=CGNotes_Normalized;User Id=maru;Password=627864;Encrypt=False;TrustServerCertificate=True;MultipleActiveResultSets=True;Connection Timeout=30"
    networks:
      - growthnote-network

networks:
  growthnote-network:
    driver: bridge
